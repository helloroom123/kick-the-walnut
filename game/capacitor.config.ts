import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kick.walnut',
  appName: '痛扁核桃',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
